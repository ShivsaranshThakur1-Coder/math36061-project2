\documentclass[a4paper,11pt]{article}

\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{siunitx}
\usepackage{hyperref}
\geometry{margin=2.5cm}
\sisetup{round-mode=figures,round-precision=3}

\title{MATH36031 Project 2:\\Fox--Rabbit Chase on a Circular Fence}
\author{Student ID: 10916801} % replace XXXXXXXX with your ID before submission
\date{\today}

\begin{document}
\maketitle

\section{Introduction}

We consider a pursuit problem on a circular fence of radius \SI{800}{\metre}. A fox starts at the origin $F_0=(0,0)$ and a rabbit starts at $R_0=(0,800)$ on the fence. The rabbit runs along the fence towards a fixed burrow located at
\[
B = 800\bigl(\sin(\pi/3),\cos(\pi/3)\bigr),
\]
while the fox aims to catch the rabbit by exploiting a gap in the fence at $G=(0,300)$ and a straight fence segment between $A=(380,600)$ and $E=(450,320)$. The fox is deemed to have captured the rabbit if the distance between them becomes less than \SI{0.1}{\metre}.

The project has two parts:
\begin{itemize}
  \item \textbf{Question 1 (constant speeds):} the fox and rabbit move with constant speeds
  \[
    s_f=\SI{16}{m/s}, \qquad s_r=\SI{13}{m/s}.
  \]
  \item \textbf{Question 2 (diminishing speeds):}
  \begin{align*}
    s_f(d_f) &= s_{f0} e^{-\mu_f d_f}, &
    s_r(d_r) &= s_{r0} e^{-\mu_r d_r},
  \end{align*}
  with $s_{f0}=\SI{16}{m/s}$, $s_{r0}=\SI{13}{m/s}$, $\mu_f=2\times 10^{-4}$ and $\mu_r=8\times 10^{-4}$, where $d_f$ and $d_r$ are the distances travelled by the fox and rabbit respectively.
\end{itemize}
The aim is to determine whether the fox captures the rabbit before it reaches the burrow in each regime, and to quantify the time and distance travelled by the fox.

\section{Mathematical formulation}

\subsection{Geometry}

The circular fence has radius $R=\SI{800}{\metre}$. We use a global Cartesian frame with the origin at $F_0=(0,0)$ and the initial rabbit position
\[
R_0 = (0,R) = (0,800).
\]
We parameterise the fence by angle $\theta$ with
\[
(x(\theta),y(\theta)) = \bigl(R\sin\theta, R\cos\theta\bigr), \qquad \theta\in[0,2\pi).
\]
The rabbit starts at $\theta=0$ and runs towards the burrow at angle $\theta_B=\pi/3$. The total arc length to the burrow is
\[
S_B = R\theta_B = 800\frac{\pi}{3} \approx \SI{837.76}{\metre}.
\]

The fox always moves in the plane. Its motion is piecewise defined by target points:
\begin{itemize}
  \item \textbf{Phase 1:} run directly from $F_0$ to the gap $G$.
  \item \textbf{Phase 2:} from then on, if the line segment from the fox to the rabbit intersects the fence segment $AE$, the fox runs towards $A$; otherwise it runs directly towards the rabbit.
\end{itemize}

\subsection{Rabbit motion}

In the constant-speed case the rabbit moves at speed $s_r=\SI{13}{m/s}$, so its arc length at time $t$ is $s(t)=s_r t$ and the angular position is
\[
\theta(t) = \min\left(\frac{s_r t}{R},\theta_B\right).
\]
The corresponding Cartesian position is
\[
R(t) = \bigl(x_r(t),y_r(t)\bigr)
       = \bigl(R\sin\theta(t), R\cos\theta(t)\bigr).
\]
The time to reach the burrow is
\[
T_B^{\text{const}} = \frac{S_B}{s_r}
= \frac{800(\pi/3)}{13} \approx \SI{64.44}{\second}.
\]

In the diminishing-speed case we model the rabbit by its cumulative distance $d_r(t)$ along the fence, evolving as
\[
\frac{d d_r}{dt} = s_r(d_r) = s_{r0} e^{-\mu_r d_r}.
\]
The position is then obtained from the distance $d_r$ via
\[
\theta(t) = \min\left(\frac{d_r(t)}{R},\theta_B\right), \qquad
R(t)= \bigl(R\sin\theta(t),R\cos\theta(t)\bigr),
\]
with the angle clamped at $\theta_B$ for $d_r\ge S_B$.

\subsection{Fox motion}

Let $F(t)=(x_f(t),y_f(t))$ denote the fox position. At any time we choose a target point $T(t)$ according to the phase logic described above. The (unit) direction of motion is
\[
\hat{u}(t) = \frac{T(t)-F(t)}{\|T(t)-F(t)\|},
\]
whenever $F(t)\neq T(t)$; otherwise the velocity is set to zero.

In the constant-speed case the fox moves with
\[
\frac{dF}{dt} = s_f\,\hat{u}(t), \qquad s_f = \SI{16}{m/s}.
\]

In the diminishing-speed case we introduce the cumulative fox distance $d_f(t)$ and set
\[
\frac{d d_f}{dt} = s_f(d_f) = s_{f0} e^{-\mu_f d_f},\qquad
\frac{dF}{dt} = s_f(d_f)\,\hat{u}(t),
\]
with $s_{f0}=\SI{16}{m/s}$ and $\mu_f=2\times 10^{-4}$.

\subsection{Capture and stopping conditions}

The simulation is stopped when one of the following events occurs:
\begin{itemize}
  \item \textbf{Capture:} $\|F(t)-R(t)\| \leq \SI{0.1}{\metre}$.
  \item \textbf{Rabbit reaches burrow:} in the constant-speed case, $t \geq T_B^{\text{const}}$; in the diminishing-speed case, $d_r(t)\geq S_B$.
\end{itemize}
We record the stopping time $T$, the fox position $F(T)$ and the distances $d_f(T)$ (and $d_r(T)$ in Question 2).

\section{Numerical method}

All simulations were implemented in \textsc{MATLAB} using the built-in solver \texttt{ode45}, an explicit Runge--Kutta (4,5) method with adaptive time stepping. The implementation is structured into small, unit-tested components under \texttt{src/} with tests under \texttt{tests/}:

\begin{itemize}
  \item Geometric primitives: segment intersection and line-of-sight blocking for the fence segment $AE$.
  \item Target selection logic: a module that maps $(F,R)$ and phase to the target point $T$.
  \item Kinematics: separate modules for constant-speed and distance-dependent-speed models for both fox and rabbit.
  \item Dynamics: ODE right-hand sides and event functions for both questions.
  \item High-level drivers: \texttt{simulate\_constant\_case} and \texttt{simulate\_variable\_case} orchestrate the phases (running to $G$, then chasing) and assemble full trajectories.
\end{itemize}

For both questions, the ODE solver is used with relative tolerance \texttt{RelTol = 1e-8} and absolute tolerance \texttt{AbsTol = 1e-10} in the main experiments. Events are implemented via custom event functions passed through \texttt{odeset}. All tests are executed using the \texttt{matlab.unittest} framework via a single script \texttt{run\_tests.sh}, which can be used in continuous integration.

\section{Implementation and code structure}

The code base is organised in a modular way under a single \texttt{src/} directory, with unit tests in \texttt{tests/}. The key entry points are:
\begin{itemize}
  \item \texttt{simulate\_constant\_case.m}, which implements the constant-speed fox--rabbit chase used to answer Question~1.
  \item \texttt{simulate\_variable\_case.m}, which implements the diminishing-speed model used in Question~2.
  \item \texttt{plot\_constant\_case.m} and \texttt{plot\_variable\_case.m}, which generate the path plots in Figures~\ref{fig:constant_paths} and~\ref{fig:variable_paths}.
  \item \texttt{export\_all\_figures.m}, called via the shell script \texttt{export\_figures.sh}, which regenerates all report figures in a reproducible way.
  \item \texttt{animate\_constant\_case.m} and \texttt{animate\_variable\_case.m}, called via \texttt{export\_animations.sh}, which produce the animations described in Section~\ref{sec:visualisations}.
\end{itemize}

Unit tests can be run directly inside \textsc{MATLAB} via \texttt{runtests('tests')}. On my system I also use the shell script \texttt{run\_tests.sh}, which invokes the \texttt{matlab.unittest} framework in headless mode via \texttt{matlab -batch}. Profiling and step counts for both regimes are collected by \texttt{profile\_simulations.m}, called through \texttt{run\_profiling.sh}. The tolerance study in Table~\ref{tab:tolerance} is driven by \texttt{convergence\_study.m} and a tolerance-parameterised driver \texttt{simulate\_constant\_case\_tol.m}.

\section{Verification and validation}

\subsection{Component-level tests}

Each key component is verified in isolation:

\begin{itemize}
  \item Geometry: line-segment intersection and blocking are tested on configurations where the correct outcome (intersect / not intersect) is known analytically.
  \item Rabbit motion: the mapping from time (or distance) to angular position and Cartesian coordinates is tested at $t=0$, at the burrow time and beyond, ensuring correct clamping at the burrow.
  \item Fox logic: target selection in both phases and the corresponding velocity direction are tested, including cases where the line-of-sight is and is not blocked by the fence segment.
  \item ODE and events: the constant-speed ODE is tested to ensure that the burrow event is triggered at $t\approx T_B^{\text{const}}$ and that the variable-speed ODE triggers the burrow event when $d_r\approx S_B$.
\end{itemize}

This test suite is run as a single batch via \texttt{runtests('tests')} and, on my system, via the shell script \texttt{run\_tests.sh}.

\subsection{Tolerance study for the constant-speed case}

To confirm that the numerical results are stable under tolerance refinement, we re-ran the constant-speed simulation for several pairs of solver tolerances $(\text{RelTol},\text{AbsTol})$ using a tolerance-parameterised driver. The tightest run
\[
\text{RelTol} = 10^{-10},\qquad \text{AbsTol} = 10^{-12}
\]
is taken as a reference, yielding
\[
T_\text{ref} = 64.44292623\ \text{s},\qquad d_{f,\text{ref}} = 1031.086820\ \text{m},
\]
with 654 stored time steps. The other runs give the results shown in Table~\ref{tab:tolerance}.

\begin{table}[h]
\centering
\begin{tabular}{cccccc}
\hline
RelTol & AbsTol & $T$ (s) & $d_f$ (m) & steps & $|T-T_\text{ref}|$ \\
\hline
$10^{-6}$  & $10^{-8}$  & 64.44292623 & 1031.086820 & 262 & 0 \\
$10^{-8}$  & $10^{-10}$ & 64.44292623 & 1031.086820 & 402 & 0 \\
$10^{-10}$ & $10^{-12}$ & 64.44292623 & 1031.086820 & 654 & 0 \\
\hline
\end{tabular}
\caption{Tolerance study for the constant-speed case: all tolerance pairs produce identical $T$ and $d_f$ to machine precision, with increasing step counts as tolerances are tightened.}
\label{tab:tolerance}
\end{table}

Within machine precision, the terminal time and fox distance are independent of the tolerances used. This confirms that the reported values are not numerical artefacts of a particular tolerance choice.

\subsection{Performance and computational cost}

We measured the computational cost of the main simulations using a simple profiling script. On a standard laptop, the constant-speed and diminishing-speed simulations yielded:

\begin{itemize}
  \item Constant-speed case: $T\approx \SI{64.44}{\second}$, $d_f\approx\SI{1031.09}{\metre}$, with 402 stored time steps and a wall-clock runtime of approximately \SI{0.19}{\second}.
  \item Diminishing-speed case: $T\approx \SI{91.79}{\second}$, $d_f\approx\SI{1287.67}{\metre}$, with approximately $2.4\times10^{7}$ stored time steps and a wall-clock runtime of roughly \SI{46}{\second}.
\end{itemize}

The dramatic increase in step count and runtime for the diminishing-speed case is expected: both animals slow down as they accumulate distance, so the system evolves over a longer physical time interval and \texttt{ode45} must take many small steps to maintain the requested accuracy.

\section{Results}

\subsection{Constant-speed case (Question 1)}

In the constant-speed regime, the rabbit reaches the burrow in
\[
T_B^{\text{const}} \approx \SI{64.44}{\second}.
\]
The numerical simulation confirms this value to within solver tolerance. The fox distance travelled by this time is
\[
d_f(T_B^{\text{const}}) \approx \SI{1031.09}{\metre},
\]
and its position is approximately
\[
F(T_B^{\text{const}}) \approx (\SI{585.51}{\metre},\,\SI{492.84}{\metre}).
\]
The separation between fox and rabbit at this time remains well above the capture threshold of \SI{0.1}{\metre}. Thus, in the constant-speed case
\[
\text{the fox does not capture the rabbit before it reaches the burrow.}
\]

Figure~\ref{fig:constant_paths} shows the trajectories of the fox and rabbit, together with the circular fence, the gap $G$, the fence segment $AE$ and the burrow.

\begin{figure}[h]
\centering
\includegraphics[width=0.7\textwidth]{constant_case_paths.png}
\caption{Fox and rabbit paths in the constant-speed case. The fox runs to the gap $G$ and then attempts to chase, but the rabbit reaches the burrow first.}
\label{fig:constant_paths}
\end{figure}

\subsection{Diminishing-speed case (Question 2)}

In the diminishing-speed regime, the cumulative rabbit distance reaches the required arc length $S_B\approx\SI{837.76}{\metre}$ at time
\[
T^{\text{var}} \approx \SI{91.79}{\second},
\]
which is significantly larger than $T_B^{\text{const}}$ due to the exponential decay of the rabbit's speed. The simulation yields
\begin{align*}
  d_f(T^{\text{var}}) &\approx \SI{1287.67}{\metre}, \\
  d_r(T^{\text{var}}) &\approx \SI{837.76}{\metre}, \\
  F(T^{\text{var}})   &\approx (\SI{0}{\metre},\,\SI{300}{\metre}) = G.
\end{align*}
Thus, the fox ends up at the gap $G$ precisely when the rabbit reaches the burrow, but again the separation is larger than \SI{0.1}{\metre}, so no capture occurs:
\[
\text{the fox does not capture the rabbit in the diminishing-speed case either.}
\]

Figure~\ref{fig:variable_paths} shows the trajectories in the variable-speed case. Compared to Figure~\ref{fig:constant_paths}, both fox and rabbit spend longer near their respective targets due to the decaying speeds, but the qualitative geometry remains similar.

\begin{figure}[h]
\centering
\includegraphics[width=0.7\textwidth]{variable_case_paths.png}
\caption{Fox and rabbit paths in the variable-speed case. Both speeds decay exponentially with distance, so the rabbit takes longer to reach the burrow, but the fox also slows, and again fails to capture.}
\label{fig:variable_paths}
\end{figure}

\subsection*{Summary of numerical results}

For clarity, Table~\ref{tab:summary} summarises the key numerical outcomes for both regimes.

\begin{table}[h]
\centering
\begin{tabular}{lcccc}
\hline
Case & $T$ (s) & $d_f(T)$ (m) & $d_r(T)$ (m) & Capture? \\
\hline
Constant speed &
$64.44$ &
$1031.09$ &
$837.76$ (burrow arc) &
No \\
Diminishing speed &
$91.79$ &
$1287.67$ &
$837.76$ (burrow arc) &
No \\
\hline
\end{tabular}
\caption{Comparison of terminal time $T$, fox distance $d_f(T)$ and rabbit distance $d_r(T)$ for the constant-speed and diminishing-speed models. In both regimes the fox fails to capture the rabbit before it reaches the burrow.}
\label{tab:summary}
\end{table}

\section{Discussion}

The results show that, in this geometric configuration, the presence of the gap $G$ and fence segment $AE$ is not sufficient for the fox to intercept the rabbit, even when it starts faster both in the constant-speed and diminishing-speed regimes. In the constant-speed case, the key limiting factor is the geometry: the rabbit effectively ``cuts the corner'' along the circle, while the fox must first reach the gap and then manoeuvre around the fence.

In the diminishing-speed case, both animals slow down with distance, but the relative advantage of the fox is not large enough to compensate for the geometric disadvantage. The fox arrives at the gap $G$ at approximately the same time that the rabbit reaches the burrow and cannot close the remaining distance before the rabbit reaches safety.

\section{Supplementary visualisations}
\label{sec:visualisations}

In addition to the static figures, we generated animations of both scenarios, showing the evolving positions of the fox and rabbit along with the fence geometry. These animations, produced via headless \textsc{MATLAB} scripts, provide further qualitative confirmation of the trajectories reported in Figures~\ref{fig:constant_paths} and~\ref{fig:variable_paths} and would be suitable as supplementary material in an electronic submission.

\section{Conclusions}

We formulated the fox--rabbit chase as a piecewise-smooth dynamical system with event-based stopping criteria and implemented it in \textsc{MATLAB} using modular, unit-tested components. For both the constant-speed and diminishing-speed models, the numerical simulations indicate that the fox fails to capture the rabbit before it reaches the burrow. The framework developed here can be extended to explore alternative parameter regimes, different fence geometries or stochastic perturbations to the motion, and already includes basic profiling and convergence tools to support such future experiments.

\appendix
\section*{Appendix: Code structure}

For reproducibility, the most important files are listed below:

\begin{itemize}
  \item \texttt{src/fox\_rabbit\_config.m} --- central configuration of speeds, geometry and tolerances.
  \item \texttt{src/simulate\_constant\_case.m} --- constant-speed simulation (Question~1).
  \item \texttt{src/simulate\_variable\_case.m} --- diminishing-speed simulation (Question~2).
  \item \texttt{src/fox\_rabbit\_ode\_const.m}, \texttt{src/fox\_rabbit\_events\_const.m} --- ODE and events for the constant-speed case.
  \item \texttt{src/fox\_rabbit\_ode\_var.m}, \texttt{src/fox\_rabbit\_events\_var.m} --- ODE and events for the variable-speed case.
  \item \texttt{tests/} --- unit tests for geometry, rabbit motion, fox logic and both simulation drivers.
\end{itemize}

The shell scripts \texttt{run\_tests.sh}, \texttt{run\_profiling.sh}, \texttt{export\_figures.sh} and \texttt{export\_animations.sh} provide single-command entry points for verification, profiling and figure/animation generation on my system; all core functionality can also be accessed directly from within \textsc{MATLAB}.

\section*{Note on the use of AI tools}

Portions of the code structure and the LaTeX report wording were developed with assistance from the AI tool ChatGPT. All mathematical modelling choices, numerical results and interpretations have been checked by the author and remain their own responsibility.

\begin{thebibliography}{9}

\bibitem{matlab:ode45}
The MathWorks, Inc.
\newblock \emph{ODE Suite: \texttt{ode45} Documentation}.
\newblock Available in the MATLAB online documentation.

\bibitem{matlab:unittest}
The MathWorks, Inc.
\newblock \emph{\texttt{matlab.unittest} Framework Documentation}.
\newblock Available in the MATLAB online documentation.

\bibitem{ascher:ode}
U.~M. Ascher and L.~R. Petzold.
\newblock \emph{Computer Methods for Ordinary Differential Equations and Differential-Algebraic Equations}.
\newblock SIAM, 1998.

\bibitem{pursuit}
W.~E. Boyce and R.~C. DiPrima,
\newblock \emph{Elementary Differential Equations and Boundary Value Problems}, John Wiley \& Sons.

\end{thebibliography}

\end{document}
